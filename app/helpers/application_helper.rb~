module ApplicationHelper

  # Return a title on a per-page basis.
  def title
    base_title = "TPlus"
    if @title.nil?
      base_title
    else
      "#{base_title} | #{@title}"
    end
  end

  def logo
	  image_tag("logo1.gif", :alt => "100", :class => "round")
  end


end
